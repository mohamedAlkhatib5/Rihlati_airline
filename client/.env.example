# Where the Vite dev server proxies /api requests.
# Leave as-is when the API runs via `php artisan serve`.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Set to true only for the hosted preview build, where there is no API.
VITE_DEMO_MODE=false
